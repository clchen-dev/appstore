# Whoami Demo（自建示例应用）

`traefik/whoami` —— 一个极简 HTTP 服务，访问后返回请求头、客户端 IP、容器主机名等信息。

常用于：测试反向代理是否正确转发、确认容器网络连通性、做最小可用的部署链路验证。

- key：`whoami-demo`（避免与官方 `whoami` 冲突）
- 镜像：`traefik/whoami`
- 容器内端口：`80`；默认对外端口 `40190`（高位端口，避免冲突，安装时可改）
- 无数据库、无持久化数据

安装后访问 `http://<服务器IP>:<你设置的端口>` 即可看到回显信息。
